# Research continuity

Work only in ReloadLightly/shinka-swarm-location. Verify the remote before any
push. Preserve the chapter-grounded objective: fixed directed routes,
OD-weighted group coverage, trip endpoints included, no duplicate trip counting.
The evolving object is deployment-search code, not a drone flight controller.

Maintain README.md as the current experiment specification: research question,
model, evolving program, data, evaluation, native configuration, run commands,
and planned analysis. Do not append session logs, milestones or historical result
blocks. Keep earlier evidence in results/ and docs/history/; distinguish planned
comparisons from completed research. Never upload the source book or keys.

Next work belongs in the evaluator, efficient search backend, and real Shinka
integration, not additional project-governance layers. Do not silently replace the
experiment. Use native islands, mutation-model bandit, inspiration sampling, and
meta-recommendations when integrating evolution. Treat the meta model as separate
from mutation-bandit routing; interpretations must follow measured evidence.
The local worker is not a security sandbox. Run generated programs in a genuinely
isolated execution environment before making hostile-code safety claims.

See docs/next_step.md for the current implementation entry points.
Use the chapter-search-v2 evaluator and keep the full program-search space.
